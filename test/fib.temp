int principal() {
    int t1 = 0, t2 = 1, nextNum = 0, n;

    #input integer as input
    int input = 2;

    tout << "Fibonacci Series: " << t1 << ", " << t2 << ", ";

    nextTerm = t1 + t2;

    while(nextNum <= n) {
        tout << nextNum << ", ";
        t1 = t2;
        t2 = nextNum;
        nextNum = t1 + t2;
    }
    return 0;
}
